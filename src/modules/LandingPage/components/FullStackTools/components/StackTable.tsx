import React from 'react';
import { FullStackToolsInfo } from '../../../../../types/LandingPage';

interface StackTableProps {
  data: FullStackToolsInfo[]
}

/**
 * Component to render my FullStack info correctly for FE and BE.
 * @param data My full stack data information 
 * @returns 
 */
const StackTable = ({ data } : StackTableProps) => {
  return (
    <>
      
    </>
  );
};

export default StackTable;